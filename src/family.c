#define R_NO_REMAP
#include <R.h>
#include <Rinternals.h>

void grp_am (int* A, int* vs, int* nlv, int* n) {
  
  int nw = 1;
  int ws[n[0]];
  int* nlw;
  nlw = 0;
  int Avs[nlv[0]];
  
  while (nlv[0] > 0) {
    nw = 0;
    nlv = 0;
    
    for (int i=0; i < nlv[0]; i++) {
      Avs[i];
    }
  }
}
